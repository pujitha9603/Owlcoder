class Solution:
    def numOfWays(self, n : int) -> int:
        # code here
        l = n // 2 -1
        if n<= 4 or n % 2 == 1:
            return 0
        else:
            a = (l // 2) * 6
            return a
