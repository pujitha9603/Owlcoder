# cook your dish here
for t in range(int(input())):
    n, m = map(int,input().split())
    a = list(map(int,input().split()))
    b = list(map(int,input().split()))
    x = n - max(b)
    ans = a[:x] + sorted(a[x:])
    print(*ans)
